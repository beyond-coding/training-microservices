spring:
  application:
    name: turbine

  cloud:
    config:
      discovery:
        enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://${EUREKA_HOSTNAME:localhost}:8761/eureka/